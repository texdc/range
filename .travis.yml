language: php

php:
  - 7.0
  - 7.1

matrix:
  fast_finish: true

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - composer update --dev --prefer-stable

script:
  - mkdir -p build/logs
  - composer test
  - composer cs

after_script:
  - sh .travis.coverage.sh
  - composer md
  - composer metrics
